{{!< default}}

{{> "header-home" }}

{{#foreach posts}}
<section class="home-image main home-style dark fullscreen" style="{{#if feature_image}}background-image:url('{{img_url}}'){{else}}background-image:url('/assets/images/empty.png'); background-repeat: repeat{{/if}}" data-url="{{url}}">
	<div class="content container small">
		<header>
			<h2>{{{title}}}</h2>
		</header>
		<p class="home-post-author"><strong>{{date format="DD MMMM YYYY"}}</strong> by {{#primary_author}}<a href="{{url}}">{{name}}</a>{{/primary_author}}</p>
	</div>
</section>
{{/foreach}}

{{pagination}}

{{> "footer-home" }}